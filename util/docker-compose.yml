# docker-compose.yml
version: "3"
services:
  database:
    image: "postgres"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=project3
      - POSTGRES_DB=glee
    ports:
      - "5532:5432"
